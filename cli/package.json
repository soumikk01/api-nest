{
  "name": "api-monitor-cli",
  "version": "1.0.0",
  "description": "CLI agent for API Monitor — intercepts HTTP calls in your dev project and streams them to the dashboard",
  "main": "dist/index.js",
  "bin": {
    "api-monitor": "dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "commander": "^12.1.0",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5"
  }
}
