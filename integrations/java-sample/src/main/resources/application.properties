server.port=8080
spring.application.name=api-monitor-java-sample

# Set these as environment variables BEFORE starting the server:
#   $env:APINEST_SDK_TOKEN="your_token"
#   $env:APINEST_BACKEND_URL="http://localhost:4000"
# DO NOT define APINEST_SDK_TOKEN= here — it causes a circular placeholder error.
